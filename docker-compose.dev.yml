# Local debugging only: docker compose -f docker-compose.yml -f docker-compose.dev.yml up
services:
  backend:
    ports:
      - "8000:8000"

  postgres:
    # Lets host-run migrations and tests reach the compose Postgres.
    ports:
      - "55432:5432"
